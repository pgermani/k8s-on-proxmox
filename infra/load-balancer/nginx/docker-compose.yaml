services:
  nginx-lb:
    image: nginx:stable
    container_name: nginx-lb
    ports:
      - "443:443"
      - "6443:6443"
    volumes:
      - ./conf/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
    networks:
      - lb_net

networks:
  lb_net:
